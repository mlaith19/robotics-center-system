Set-Location "e:\PROJECTS\robotics center system"
git add .
git commit -m "תיאור השינוי"
git push origin mainSet-Location "e:\PROJECTS\robotics center system"
git add .
git commit -m "תיאור השינוי"
git push origin main
ssh root@147.93.123.132



Set-Location "e:\PROJECTS\robotics center system"; git add .; $msg = "auto-update $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"; git commit -m $msg; git push origin main; ssh root@147.93.123.132

Set-Location "e:\PROJECTS\robotics center system"; git add .; $msg = "auto-update $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"; git commit -m $msg; git push origin main; ssh root@147.93.123.132 "cd /var/www/robotics-center && git pull origin main && (docker compose --env-file .env.production -f docker-compose.production.yml up -d --build || docker compose --env-file .env -f docker-compose.production.yml up -d --build) && docker image prune -f"


Set-Location "e:\PROJECTS\robotics center system"; $hasChanges = -not [string]::IsNullOrWhiteSpace((git status --porcelain)); if ($hasChanges) { $msg = Read-Host "הכנס שם שינוי (commit message)"; if ([string]::IsNullOrWhiteSpace($msg)) { $msg = "auto-update $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')" }; git add .; git commit -m $msg; git push origin main } else { Write-Host "אין שינויים מקומיים - מדלג על commit/push" }; ssh root@147.93.123.132 "cd /var/www/robotics-center && git pull --rebase origin main && (docker compose --env-file .env.production -f docker-compose.production.yml up -d --build || docker compose --env-file .env -f docker-compose.production.yml up -d --build) && docker image prune -f"